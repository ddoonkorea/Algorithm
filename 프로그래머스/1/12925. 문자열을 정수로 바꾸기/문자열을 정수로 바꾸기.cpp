#include <string>
#include <vector>

using namespace std;

int solution(string s) {
    int answer = 0;
    int a = stoi(s);

    if (a > 0) {
        answer = a;
    } else answer = a;
    
    return answer;
}